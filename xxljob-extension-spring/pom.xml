<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.hiwepy</groupId>
        <artifactId>xxljob-extension</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>xxljob-extension-spring</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <description>XXL-Job Spring Boot configuration properties binding</description>

    <dependencies>
        <!-- core：admin 客户端、模型、枚举、常量 -->
        <dependency>
            <groupId>io.github.hiwepy</groupId>
            <artifactId>xxljob-extension-core</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- Spring Framework：Bean 扫描、ApplicationContext、生命周期（由 spring-boot-autoconfigure 传递 beans/context/core） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <version>2.7.18</version>
        </dependency>
        <!-- Micrometer：metrics 扩展埋点（provided，由下游 starter/应用引入 actuator） -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <version>1.9.17</version>
            <scope>provided</scope>
        </dependency>
        <!-- 编译期代码生成 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- 单元测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
